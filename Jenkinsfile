pipeline {
    agent { node { label 'docker-agent-alpine' } }
    stages {
        stage('build') {
            steps {
                echo "Build stage from pipeline"
//                 sh 'python --version'
            }
        }
    }
}
